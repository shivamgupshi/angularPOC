import { AddingSpacePipe } from './adding-space.pipe';

describe('AddingSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new AddingSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
